# Repository visibility packet

**Status:** `CLEARED` (verified 2026-08-18)

Both previously private research-extension repositories are **public**:

- https://github.com/gunnchOS3k/gunnchos-gpu-nr-baseband-platform (`visibility: PUBLIC`, `isPrivate: false`)
- https://github.com/gunnchOS3k/gunnchos-emergent-service-intent-protocols (`visibility: PUBLIC`, `isPrivate: false`)

Do **not** make them private. Faculty path may cite these URLs.

GPU CUDA timings remain `BLOCKED_GPU` on CPU-only hosts. That is an evidence-class issue, not a 404.

```bash
gh repo view gunnchOS3k/gunnchos-gpu-nr-baseband-platform --json isPrivate,visibility
gh repo view gunnchOS3k/gunnchos-emergent-service-intent-protocols --json isPrivate,visibility
```
