# Recovery image procedures (digital)

1. Boot recovery (signed image).
2. Verify package provenance.
3. Options: rollback last update | reinstall OS | export diagnostics | factory reset.
4. On interrupted upgrade: resume if package intact else rollback (align with Stream A install matrix).

Physical media sanitize: EXTERNAL/PHYSICAL — not claimed here.
